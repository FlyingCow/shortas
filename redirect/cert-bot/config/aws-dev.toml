# AWS Dev Configuration for Cert Bot
# This config is used when running on AWS ECS (dev environment)

[acme]
directory_url = "https://acme-staging-v02.api.letsencrypt.org/directory"  # Staging
account_email = "admin@shortas.com"
account_key_path = "/etc/cert-bot/account.key"

[click_router_api]
base_url = "http://click-router-api.dev.shortas.local:3000"
timeout_seconds = 30

[aws]
local = false

[aws.dynamo]
orders_table = "core-certificate-orders-dev"

# MongoDB is not used in AWS - DynamoDB is the primary store
[mongodb]
uri = ""
database = ""
orders_collection = ""

# RabbitMQ URI is loaded from environment variable APP_RABBITMQ_URI
[rabbitmq]
domain_exchange = "domain.state.changed"
certificate_exchange = "cache.invalidation.certificates"
reconnect_seconds = 5

[worker]
check_interval_seconds = 30
batch_size = 5
max_retries = 3

[renewal]
check_interval_hours = 12
renewal_days_before = 30
